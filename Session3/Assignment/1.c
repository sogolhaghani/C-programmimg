// Find Errors


#include <stdio.h>

int main(){
    
    int const total = 10;
    int float = 0;
    long double y = 0.5 , d = 1.5 , z = 2.5;

    if( y => ++d ){
        printf("y : %d \n", y);
    } else if{
        printf("y : %d \n", y);
    }

for(int l = -50 ; l < total ; ++l ){

}

    for(y, y < total ; ++y ){
        printf("y = %u", y) 
    }    

    for(d; d< total ; ++y ){
        printf("d = %u", d) 
    }  

    

}